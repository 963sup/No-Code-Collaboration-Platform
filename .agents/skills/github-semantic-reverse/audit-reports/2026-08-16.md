# GitHub Semantic Audit — 2026-08-16

## Cycle identity

- Audit round: **3 — executable revalidation and causal-label repair**
- Target repository: `963sup/No-Code-Collaboration-Platform`
- Development line: `first-principles/collaboration-relationship-kernel-repair`
- Locked benchmark: `github/docs@81ade08c26f13325c0cde8a23cd3bfb85bd0778e`
- Audited parent: `522c191e1461e917c5a7511e0c1aef94dc71371e`
- Authority order: task and repository code/docs; Linear and Notion are mirrors only
- Overall status: **not passed**

Round 2 revoked the `commit` verification criterion once. Round 3 applies the corrected criterion to executable Page behavior. The mapping passes this round; no second consecutive revocation occurred.

## newly_passed

1. **`commit` revalidation** — a Page save is described only as resulting Current State under a server-managed revision. Activity Event is separate Evidence and is not used to make the save-description answer pass.
2. **Executable exclusion audit** — all nine excluded concepts were checked against Product, Domain, Application, SQL/persistence, URL/IA, UI, fixtures, and tests visible through the connected repository. No excluded Product primitive or hidden source-control-shaped object model was found.
3. **Eight-concept Product audit** — Enterprise, Organization, Team, Collaborator, User/Social, Wiki, Projects, and Issues were checked against locked benchmark evidence and current executable shape.
4. **Collaborator causal correction** — Direct User Grant remains the explicit authority Relationship; collaborator remains derived effective access. Access-management UI and Domain wording no longer make collaborator the stored cause.
5. **Issue author correction** — Issue UI now attributes the actual creator and does not classify every author or public participant as collaborator.
6. **Current-state revision acceptance** — `version`, `expectedVersion`, `expected_version`, `updatedAt`, and `expectedUpdatedAt` are accepted only as scalar current-state concurrency projections with no history identity, ancestry, snapshot payload, alternate state line, or authority.
7. **Wiki verification** — `/wiki` is Page/Knowledge presentation over the same Repository containment, command, concurrency, and Evidence contracts.
8. **Projects verification** — Project surfaces are read Projections over authorized work and own no Artifact, Repository, Grant, or source mutation.
9. **Issue verification** — assignment requires `issue.manage`, and an assignee must already be able to view the Repository; assignment creates no authority.

## maintained_passed

1. Reference snapshot remains locked to one upstream commit and date.
2. `GLOSSARY.md` remains the only excluded-concept mapping authority.
3. `BENCHMARK_CONCEPT_MATRIX.md` covers all eight names enumerated by v2.
4. Repository code/docs remain authoritative; Linear and Notion remain mirrors.
5. Product/Ontology/ADR-014 retain the current-state collaboration kernel.
6. ADR-013 and the generic structured-data-change envelope remain superseded.
7. Typed transfer and Repository duplication remain separate deferred candidates with no inherited authority or synchronization.
8. Engineering Git/GitHub workflow, SQL transaction syntax, benchmark evidence, and superseded history remain legitimate external-reference contexts.

## revoked

None.

The round-2 `commit` revocation is resolved by cross-round revalidation. Its revocation count remains one historically and is not active in round 3.

## not_passed

1. **Full repository commands not executed** — this connector-only environment has no complete checkout, so `pnpm codex:check` and `pnpm verify:fast` have not run against the resulting tree.
2. **Remote checks unavailable** — no GitHub Actions workflow run exists for the direct branch commits. The visible Vercel `build-rate-limit` failure predates this repair and cannot validate or invalidate Product semantics.
3. **Schema comment normalization pending** — `supabase/schemas/30_repository.sql` still says `authorization/history boundary`. Its shape contains no history object, but the wording should become `authorization/Evidence boundary` together with the replaceable local baseline and replay verification.
4. **Cycle convergence not reached** — `not_passed` is non-empty even though `revoked` is empty.

## Excluded-concept mapping status

| External concept | Canonical outcome | Round 3 |
| --- | --- | --- |
| `commit` | Current State Transition only; Activity Event is orthogonal Evidence | Passed after revalidation |
| `branch` | serialization, real-time collaboration, or Expected Revision; no alternate state line | Passed |
| `diff` | optional derived State Comparison over independently justified retained states | Passed |
| `merge` | no Product convergence primitive; owning Resource command establishes Current State | Passed |
| `fork` | future independent Repository duplication only; no upstream authority or synchronization | Passed |
| `rebase` | rejected; no history rewrite | Passed |
| `cherry-pick` | rejected; future typed movement is separately authorized | Passed |
| `tag` | rejected as history reference; Label is classification only | Passed |
| `HEAD` | rejected; stable identity resolves Current State | Passed |

## Product benchmark status

| Benchmark concept | Canonical outcome | Round 3 |
| --- | --- | --- |
| Enterprise | deferred cross-Organization governance Scope | Passed |
| Organization | canonical identity/admin Scope and possible Repository Owner; not Actor/Container | Passed |
| Team | deferred Organization-scoped group Principal | Passed |
| Collaborator | derived effective-access classification | Passed after causal-label repair |
| User / Social | canonical human identity; profile/social surfaces are Presentation | Passed |
| Wiki | `/wiki` presentation over Repository-contained Page/Knowledge | Passed |
| Projects | non-owning planning Projection | Passed |
| Issues | Repository-scoped actionable Resource | Passed |

## Root-correction record

| File | Incorrect boundary | Correction |
| --- | --- | --- |
| `docs/domains/access-authority.md` | “Direct collaborator relationship” made a derived classification look causal | Rebuilt around explicit Direct User Grant and derived Collaborator |
| `apps/web/.../settings/access/page.tsx` | Access management labeled stored Grants as Direct collaborators | Renamed to Direct User grants and Grant access |
| `apps/web/.../_components/issue-detail.tsx` | Every Issue creator was labeled Collaborator | Attribute actual `createdBy` identity without collaborator inference |
| `.agents/skills/github-semantic-reverse/EXECUTABLE_SEMANTIC_AUDIT.md` | No single executable evidence ledger covered round 3 | Added context-aware concept-by-concept evidence |
| `tooling/check-github-semantic-contract.mjs` | Round-2 checker required an active revocation and did not guard causal UI labels | Advance to round 3 and reject false collaborator labels |

## Source-of-truth synchronization log

- Linear `963-7` records the round-2 revocation and may close only after this repository correction is committed.
- Linear `963-5` and `963-6` remain the process-kernel and Product-semantic parent work.
- Notion remains a non-authoritative mirror and will be appended after the repository commit.
- No mirror wording was used to override repository code or contracts.

## Next repair boundary

Use a complete checkout to run the repository verification commands. Then normalize the Repository table comment together with the local baseline and replay evidence. When both are complete, create a new audit round with `revoked = 0` and `not_passed = 0`; only then may the repair cycle be marked passed or integrated to `main`.
