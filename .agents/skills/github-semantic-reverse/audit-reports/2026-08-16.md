# GitHub Semantic Audit — 2026-08-16

## Cycle identity

- Audit round: **2 — verification-criterion correction**
- Target repository: `963sup/No-Code-Collaboration-Platform`
- Development line: `first-principles/collaboration-relationship-kernel-repair`
- Authoritative execution input: v2 correction specification
- Superseded historical input: v1 specification
- Locked benchmark: `github/docs@81ade08c26f13325c0cde8a23cd3bfb85bd0778e`
- Authority order: current task and repository code/docs; Linear and Notion are mirrors only
- Previous repository round: commit `bebdf569876fbc0f15a9dfd2ce9a73735af39ae7`
- Overall status: **not passed**

This round contains a revocation. Under the v2 feedback loop, implementation work for the revoked item is paused until its verification scenario is corrected in `GLOSSARY.md`. The corrected item must be revalidated in the next audit round; it cannot be declared passed in the same round that revokes the prior criterion.

## newly_passed

1. **Locked exclusion evidence coverage** — `REFERENCE_SNAPSHOT.md` now maps all nine excluded external concepts to concrete paths at the locked `github/docs` commit.
2. **Locked Product benchmark coverage** — `BENCHMARK_CONCEPT_MATRIX.md` covers every name enumerated by v2: Enterprise, Organization, Team, Collaborator, User/Social, Wiki, Projects, and Issues. The input says seven but lists eight; the non-lossy resolution audits all eight.
3. **Explicit verification contract** — every excluded concept now has a target outcome, data-versioning classification, verification question, required answer, and forbidden answer shape in the single canonical `GLOSSARY.md`.
4. **Current-state revision boundary** — `State Revision` and `Expected Revision` are defined as scalar concurrency semantics. Existing `version`, `expectedVersion`, and `expected_version` identifiers pass only while they own no history identity, snapshot payload, ancestry, alternate state line, URL, author, message, or authority.
5. **Context-aware executable scan** — searches found no Product/Domain/Application/API/schema/route/UI identifiers for Data Commit, Data Branch, Data Diff, Change Proposal, Repository Derivation, Data Capsule, commit object identity, branch identity, history node, upstream Repository relationship, or fork origin.
6. **Legitimate external-reference classification** — current `commit`, `branch`, `diff`, `merge`, `fork`, `rebase`, `tag`, and `HEAD` word hits are confined to Git/Codex engineering workflow, SQL transaction syntax, locked benchmark evidence, superseded decision history, or explicit exclusion rules. The executable `twMerge` hit is CSS-class composition, not Product semantics.
7. **Vercel evidence classification** — the pre-repair baseline commit already reported the same Vercel `build-rate-limit` failure. The connected Vercel Team cannot read the separate dashboard namespace. This is external quota/access evidence, not a semantic Product regression.

## maintained_passed

Maintained from audit round 1 in this file's Git history and commits `147ba8e7676fd176f5b8c90ac45aec57d95f0f49` and `bebdf569876fbc0f15a9dfd2ce9a73735af39ae7`:

1. **Reference snapshot lock** — one immutable upstream revision, date, and `content/` + `src/` coverage remain fixed for the cycle.
2. **Single mapping authority** — `GLOSSARY.md` remains the only excluded-concept mapping authority.
3. **Semantic-context boundary** — Product/Domain/Application/UI admission remains separate from benchmark, audit, repair, and engineering workflow references.
4. **Source-of-truth arbitration** — repository code and documentation remain authoritative; Linear and Notion follow them.
5. **Canonical Product/Ontology correction** — current Product truth continues to use concrete Resource commands, Expected Revision when required, State Transition, authoritative Current State, and separate Activity Event Evidence.
6. **Architecture correction** — ADR-013 remains Superseded; ADR-014 remains Accepted and owns the current-state collaboration kernel.
7. **Domain correction** — the generic structured-data-change envelope remains Superseded; controlled data movement remains Deferred with no accepted Product capability.
8. **Future transfer/copy gate** — typed movement and Repository duplication still require separate Product decisions and cannot inherit Grants, credentials, Sessions, secrets, tokens, synchronization, or upstream authority.

## revoked

### `commit` verification scenario — revoked once

- **Previously accepted criterion:** a save was described as an accepted State Transition and the same verification answer also said that the system records an Activity Event.
- **Why revoked:** v2 explicitly requires the answer to “使用者在容器中儲存了一次變更，系統怎麼描述這個動作？” to avoid a “who changed what and when” narrative. Although Activity Event was modeled separately, mentioning it in the same save-description answer allowed the criterion to pass itself through historical Evidence.
- **Criterion correction:** the answer now describes only resulting `Current State` and `State Revision`. Activity Event is orthogonal Evidence, is not the replacement for the excluded concept, and may be inspected only by a separate audit question.
- **Corrected required answer:** `The Page Current State now contains the accepted title and body at State Revision 12.`
- **Implementation effect this round:** none. Current executable mutation already writes current Resource state with optimistic concurrency and separate Activity Event Evidence; the defect was in the verification criterion, so v2 requires correcting the criterion before touching implementation.
- **Revocation count:** 1. A second consecutive revocation of this same mapping would stop automatic redefinition and require an explicit 963sup Product decision.
- **Tracking:** Linear `963-7`; Notion audit mirror updated as non-authoritative and in progress.

## not_passed

1. **`commit` revalidation pending** — the corrected criterion must be run in a new audit round. It cannot move directly from revoked to passed in this round.
2. **Eight-concept target verification pending** — the new benchmark matrix must be checked against current Product/Domain/Application/schema/route/UI evidence in the next round.
3. **Full repository verification unavailable** — no full local checkout was available to run `pnpm codex:check` or `pnpm verify:fast`, and no GitHub Actions workflow run exists for the direct branch commits.
4. **Final mirror synchronization pending** — Linear and Notion must be updated after the next repository audit round establishes the resulting code/docs state.
5. **Cycle convergence not reached** — `revoked` and `not_passed` are non-empty.

## Excluded-concept mapping status

| External concept | Canonical outcome | Verification contract | This round |
| --- | --- | --- | --- |
| `commit` | Current State Transition only; no Product history primitive | Corrected | Revoked; revalidation pending |
| `branch` | serialization, real-time collaboration, or Expected Revision; no alternate state line | Explicit | Maintained pending full recheck |
| `diff` | optional State Comparison Projection over independently justified retained states | Explicit | Maintained pending full recheck |
| `merge` | no Product primitive; owning Resource concurrency contract establishes Current State | Explicit | Maintained pending full recheck |
| `fork` | future independent Repository Duplication only; no upstream authority or sync | Explicit | Maintained pending full recheck |
| `rebase` | rejected; no history rewrite | Explicit | Maintained pending full recheck |
| `cherry-pick` | rejected; future typed movement is separately authorized Data Transfer | Explicit | Maintained pending full recheck |
| `tag` | rejected as history reference; Label remains classification only | Explicit | Maintained pending full recheck |
| `HEAD` | rejected; stable identity resolves Current State | Explicit | Maintained pending full recheck |

## Product benchmark status

| Benchmark concept | Locked-source trace | Canonical target status | This round |
| --- | --- | --- | --- |
| Enterprise | Complete | Deferred governance Scope; no direct Repository ownership/content authority | Matrix added; executable recheck pending |
| Organization | Complete | Canonical identity/admin Scope and possible Repository Owner; not Actor/Container | Matrix added; executable recheck pending |
| Team | Complete | Deferred Organization-scoped group Principal | Matrix added; executable recheck pending |
| Collaborator | Complete | Derived effective-access classification | Matrix added; executable recheck pending |
| User / Social | Complete | User identity canonical; profile/social data is Presentation only | Matrix added; executable recheck pending |
| Wiki | Complete | `/wiki` presentation over Page/Knowledge | Matrix added; executable recheck pending |
| Projects | Complete | Non-owning planning Projection; entity/lifecycle deferred | Matrix added; executable recheck pending |
| Issues | Complete | Canonical Repository-scoped actionable Resource | Matrix added; executable recheck pending |

## Root-correction record

| File | Incorrect boundary | Root correction | Mapping item |
| --- | --- | --- | --- |
| `.agents/skills/github-semantic-reverse/GLOSSARY.md` | Save verification could pass by mentioning historical Evidence | Replaced with exact question, state-only required answer, forbidden answer shape, and separate Evidence rule | `commit` |
| `.agents/skills/github-semantic-reverse/REFERENCE_SNAPSHOT.md` | Locked commit existed but excluded concepts lacked per-concept source trace | Added concrete locked paths and external assumptions for all nine exclusions | all excluded concepts |
| `.agents/skills/github-semantic-reverse/BENCHMARK_CONCEPT_MATRIX.md` | Required Product comparison names lacked one traceable matrix | Added all eight names, surviving problem, removed assumptions, target outcome, and falsifying test | Enterprise through Issues |
| `.agents/skills/github-semantic-reverse/SKILL.md` | Workflow did not require the matrix or exact verification answer contract | Added mandatory matrix read and revoked-item revalidation rule | process kernel |
| `tooling/check-github-semantic-contract.mjs` | Checker verified headings/terms but could not detect the false-pass save scenario | Add section-level verification checks, state-revision boundary checks, matrix coverage, and revoked-round checks | process kernel + `commit` |

## Source-of-truth synchronization log

1. **Found mismatch:** repository glossary allowed Activity Event in the same save-description verification answer while v2 prohibited historical narration in that answer.
2. **Authority decision:** v2 task input plus repository code/docs are authoritative; Linear and Notion cannot make the old criterion valid.
3. **Mirror correction:** Linear `963-7` created as In Progress and urgent; Notion audit page appended with the revocation reason and correction direction.
4. **No reverse synchronization:** no Product or code definition was changed to match mirror wording.
5. **External status evidence:** Vercel failure predates this repair and points to build-rate limiting; no Product correction is derived from it.

## Next repair boundary

Start audit round 3 by re-running the corrected `commit` scenario against current code/docs. Then verify all eight benchmark concepts and all nine excluded concepts against executable identifiers, schema, routes, user-facing labels, icons, and interaction shapes. Only after that round has `revoked = 0` and `not_passed = 0` may the cycle be marked passed or merged to `main`.
