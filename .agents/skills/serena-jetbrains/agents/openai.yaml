interface:
  display_name: "Serena JetBrains"
  short_description: "Verify exposed tools; choose Serena or built-ins."
  default_prompt: "Use $serena-jetbrains to verify the exposed Serena tools, choose the semantic or built-in path, and make the smallest safe change."
dependencies:
  tools:
    - type: "mcp"
      value: "serena"
      description: "Local Serena MCP server backed by the open JetBrains project."
policy:
  allow_implicit_invocation: true
