interface:
  display_name: "GitHub Product Semantics"
  short_description: "Extract durable GitHub mechanisms without copying surface design."
  default_prompt: "Research this GitHub concept as benchmark evidence, then derive the smallest no-code platform model that independently requires it."
policy:
  allow_implicit_invocation: true
