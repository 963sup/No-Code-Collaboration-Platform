interface:
  display_name: "GitHub Product Semantics"
  short_description: "Extract durable GitHub mechanisms without surface copying."
  default_prompt: "Use $github-product-semantics to research this GitHub concept as benchmark evidence and derive the smallest independently required no-code model."
policy:
  allow_implicit_invocation: true
