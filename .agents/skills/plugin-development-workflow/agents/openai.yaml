interface:
  display_name: "Plugin Development Workflow"
  short_description: "Route seven development plugins by truth class and evidence boundary."
  default_prompt: "Run this development task through the repository's plugin orchestration contract, using only the connected tools that are necessary and preserving repository truth boundaries."
policy:
  allow_implicit_invocation: true
