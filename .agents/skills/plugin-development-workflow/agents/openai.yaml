interface:
  display_name: "Plugin Development Workflow"
  short_description: "Route development plugins by truth class and evidence boundary."
  default_prompt: "Use $plugin-development-workflow to route this task through only the necessary connected tools while preserving repository truth boundaries."
policy:
  allow_implicit_invocation: true
