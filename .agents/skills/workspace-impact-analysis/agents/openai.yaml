interface:
  display_name: "Workspace Impact Analysis"
  short_description: "Map pnpm and Turbo package impact before cross-package changes."
  default_prompt: "Use $workspace-impact-analysis to map this change across the pnpm workspace and Turbo task graph, then report affected packages and checks."
policy:
  allow_implicit_invocation: true
