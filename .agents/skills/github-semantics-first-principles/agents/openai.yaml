interface:
  display_name: "GitHub Semantics First Principles"
  short_description: "Audit GitHub-derived semantics against the Repository axiom."
  default_prompt: "Use $github-semantics-first-principles to audit this GitHub-derived concept against the Repository axiom and no-code exclusion boundaries."
policy:
  allow_implicit_invocation: true
