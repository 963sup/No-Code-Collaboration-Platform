# Core

- Truth router: `docs/README.md`; current Product, Ontology, Architecture, and gap contracts outrank code snapshots and memories.
- Absolute Product axiom: **Repository = No-Code Collaboration Container**. GitHub is benchmark evidence only; code-specific meaning is not admitted by renaming.
- Workspace map: `apps/web` delivery/composition; `packages/application` use cases and ports; `packages/domain` provider-neutral business truth; `packages/infrastructure/supabase` port implementations and generated DB projection; `packages/ui` presentation primitives; `supabase/schemas` desired database truth.
- Dependency direction and cross-cutting trust boundaries: `mem:project/architecture_truth_boundaries`.
- Repository-create authorization contract: `mem:domain/repository_creation_authority`.
- Schema and migration lifecycle: `mem:database/supabase_schema_lifecycle`.
- Toolchain and version pins: `mem:tech_stack`. Routine commands: `mem:suggested_commands`. Completion gates: `mem:task_completion`.
