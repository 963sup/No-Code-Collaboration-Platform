# Core

- Truth router: `docs/README.md`; current Product, Ontology, Architecture, and gap contracts outrank executable snapshots and memories.
- Absolute Product axiom: **Repository = No-Code Collaboration Container**. GitHub is benchmark evidence only; software-development-specific meaning is not admitted by renaming.
- Workspace ownership: `apps/web` delivery/composition; `packages/application` use cases and provider-neutral ports; `packages/domain` provider-neutral business truth; `packages/infrastructure/supabase` port implementations and generated database projection; `packages/ui` accessible presentation primitives; `supabase/schemas` desired database truth.
- Dependency direction and trust boundaries: `mem:project/architecture_truth_boundaries`.
- Repository-create authorization contract: `mem:domain/repository_creation_authority`.
- Schema and migration lifecycle: `mem:database/supabase_schema_lifecycle`.
- Toolchain/version pins: `mem:tech_stack`. Routine commands: `mem:suggested_commands`. Completion gates: `mem:task_completion`.
- Memory ownership, scope, and worktree discipline: `mem:memory_maintenance`.
