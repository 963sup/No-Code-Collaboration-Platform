import { SettingsPreview } from '@/components/settings-preview';
export default function ProfileSettingsPage() {
  return (
    <SettingsPreview
      description='Manage the human profile projection used for attribution and display. Profile changes never create Repository authority.'
      title='Profile'
    />
  );
}
