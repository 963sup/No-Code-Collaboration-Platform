import { SettingsPreview } from '@/components/settings-preview';

export default function TokensSettingsPage() {
  return (
    <SettingsPreview
      availability='deferred'
      description='Observed GitHub programmatic-access information architecture only. Product token issuance, scopes, credentials, and machine authority are not established.'
      title='Developer settings'
    />
  );
}
