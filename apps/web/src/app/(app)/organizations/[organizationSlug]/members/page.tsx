import { SurfaceFrame } from '@/components/surface-frame';
export default function OrganizationMembersPage() {
  return (
    <SurfaceFrame
      availability='preview'
      description='Organization Membership governance. This preview does not claim that Membership grants Repository access.'
      emptyTitle='Member administration is not live'
      title='Organization members'
    />
  );
}
