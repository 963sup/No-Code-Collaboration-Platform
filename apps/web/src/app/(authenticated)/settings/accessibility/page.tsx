import { SettingsPreview } from '@/components/settings-preview';
export default function AccessibilitySettingsPage() {
  return (
    <SettingsPreview
      description='Accessibility preferences affect presentation without changing Product identity, Artifact state, or authority.'
      title='Accessibility'
    />
  );
}
