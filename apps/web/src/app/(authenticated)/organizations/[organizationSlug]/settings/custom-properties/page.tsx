import { SurfaceFrame } from '@/components/surface-frame';
export default function OrganizationPropertiesPage() {
  return (
    <SurfaceFrame
      availability='preview'
      description='Custom metadata intent for no-code collaboration. No untyped schema or arbitrary expression engine is created by this preview.'
      emptyTitle='Custom properties are not live'
      title='Custom properties'
    />
  );
}
