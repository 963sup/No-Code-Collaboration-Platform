# Status dialog

- Opened from `Set status` in the user menu without changing the URL.
- Contains emoji, free text with character count, suggested statuses, busy flag, expiration selection, and visibility scope (`Everyone` or `ac-sup`).
- Status is actor presence/presentation, not authority and not a stable URL resource.
- No value was entered and neither `Clear status` nor `Set status` was executed.
