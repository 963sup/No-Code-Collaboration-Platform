# Repository navigation

The observed owner and Repository appear together in breadcrumbs. GitHub presents Code, Issues, Pull requests, Actions, Projects, Security, Insights, and other available Repository destinations in a horizontal navigation region. On mobile, only a subset is shown directly and current/overflow items move behind `More`; selecting a destination changes the child path, while opening overflow does not.
