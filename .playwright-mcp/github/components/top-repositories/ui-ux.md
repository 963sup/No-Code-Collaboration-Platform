# Top repositories

This is a user-specific Repository discovery projection inside the Dashboard, not a new owner or Container. It combines the current account-context trigger, a local filter, a Repository creation command and links to stable Repository identities. Desktop/Laptop render it in the Account side region; Tablet preserves a compact region; Mobile moves the same projection into the main flow. Search text is temporary UI state and does not create a URL.
