# Notification indicator

- The global-header inbox icon links to `/notifications` and exposes `You have unread notifications (g then n)` through its labelled tooltip.
- Unread state is a small accent indicator on the icon; the icon remains available at all four viewports.
- The link was hovered and inventoried but not opened, avoiding a possible read-state mutation.
