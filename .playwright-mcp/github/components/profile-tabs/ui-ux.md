# Profile navigation tabs

The profile identity remains `/369sup`; Repositories, Projects, Packages, Stars, Sponsors and Achievements are projections selected by `?tab=`. The query changes visible content while preserving the same User owner. Selected state is conveyed by text, icon and underline; narrow viewports horizontally constrain the tab strip rather than inventing component URLs. Code-dependent Packages is excluded from the target.
