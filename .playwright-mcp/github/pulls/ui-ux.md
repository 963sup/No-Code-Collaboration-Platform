# Pull requests exclusion boundary

`/pulls` redirects to `/pulls/inbox`. The entry is visible in GitHub's global navigation, but Pull Request, review, Commit, Branch and Diff semantics are excluded from the no-code collaboration platform. Only the shell-level navigation evidence is retained; no Pull Request content or workflow is captured or adapted.
