# My gists exclusion boundary

`/mine` resolves the authenticated User and redirects to `/369sup`. This is an identity-aware alias on a separate host. Gist content remains excluded because it is a code-snippet domain; no Gist page screenshot or content is retained.
