# Gist exclusion boundary

`github.com/gist` transfers to the separate `gist.github.com` product surface. Its value depends on source-code/snippet storage, so the target rejects the resource and creation workflow. Only the visible create-menu entry and redirect relation are recorded.
