# Gist redirect observation

`github.com/gist` transferred to the separate `gist.github.com` product surface, which presented snippet/file creation and listing behavior. Only the visible create-menu entry and redirect relation were recorded; no content was created.
