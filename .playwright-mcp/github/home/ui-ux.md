# Public home

- Anonymous marketing and discovery entry at `/`.
- Authentication changes the projection at the same request URL; the authenticated dashboard is indexed separately under `github/dashboard/` using its menu canonical `/dashboard`.
- Responsive captures cover all four required viewports.
