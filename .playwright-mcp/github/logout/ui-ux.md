# Sign-out command

Sign out is a destructive session command rather than a bookmarkable domain resource. The menu entry and href are recorded, but navigation was intentionally not executed so the benchmark session remains read-only and authenticated.
