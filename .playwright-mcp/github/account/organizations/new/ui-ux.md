# Create organization entry

The command URL redirects to `/organizations/plan`. The entry represents the start of a creation process, not an Organization with stable identity. The resulting Organization receives its own identity only after successful creation; no write action was performed.
