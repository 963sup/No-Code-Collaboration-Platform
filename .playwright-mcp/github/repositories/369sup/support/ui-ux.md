# Repository overview

`369sup` is the User owner and `support` is the concrete Repository identity. The benchmark shell establishes ownership context, repository-level navigation and visibility/status metadata. Source Code, git refs/merge, clone, and executable workflow mechanics visible on GitHub are excluded. Commit/Branch/Diff/Proposal/Transfer names require their separate no-code structured-data contracts and are not inferred from this screenshot.

## Target comparison

The first local slice preserves the global shell, Owner/Repository identity row, visibility cue, responsive resource navigation, one active content surface, and stacked mobile detail region. It replaces GitHub's code file table with accepted no-code Repository overview content.

The circular `N` control at the lower-left of target captures is Next.js development tooling chrome, not Product UI. Production visual comparison must ignore it.

- `screenshots/target-repository-overview-desktop.png`
- `screenshots/target-repository-overview-laptop.png`
- `screenshots/target-repository-overview-tablet.png`
- `screenshots/target-repository-overview-mobile.png`
