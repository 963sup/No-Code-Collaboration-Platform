# Repository overview

`369sup` is the User owner and `support` is the concrete Repository identity. The benchmark shell establishes ownership context, repository-level navigation and visibility/status metadata. Source Code, branch, commit and clone surfaces visible on GitHub are excluded; the target retains only the collaboration-container shell.
