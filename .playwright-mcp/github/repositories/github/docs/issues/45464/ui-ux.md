# Issue 45464

- Stable identity is owner slug + Repository slug + issue number.
- The same canonical URL must serve a full page on direct load/refresh and may be intercepted into a dialog during soft navigation in the target app.
- Comments, reactions, assignees, labels, milestones, status, mentions, and timeline events belong to Issue collaboration; code-specific links remain excluded.
