# All issues entry

`/issues` is a global-navigation entry alias. An authenticated GET resolves to `/issues/assigned`, preserving the user's current issue-inbox projection. The stable domain resource is the user-scoped cross-Repository Issue collection; `assigned` is a saved projection, not a Repository child.
